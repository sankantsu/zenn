---
title: "【Wezterm】Copilot CLI のセッションをキーボードでスクロールする"
emoji: "⌨️"
type: "tech" # tech: 技術記事 / idea: アイデア
topics: ["wezterm", "githubcopilotcli"]
published: true
---

## やりたいこと

Github Copilot CLI を使っていて、 セッション内をスクロールしづらいのが UI 面でやや不満でした。
マウス (タッチパッド) ではスクロールできますが、スクロール量が制御しづらく感じます。

(私が知る限り) キーボード操作では Home/End でセッション先頭/末尾へ移動、または PageUp/PageDown でのページ単位でしかスクロールできません。
もっと細かく、行単位でのスクロールがしたいです。
https://github.com/github/copilot-cli/issues/2750

## 解決策

マウスではスクロールできるんだから、マウス操作相当の入力を keymap で送ってしまえばいいのではないかと考えました。

Wezterm では、以下のように実現できました。
この設定を行うと、 `Ctrl + Shift + ↑/↓` でマウス操作相当の control sequence を送って行単位でスクロールができるようになります。

```lua
local wezterm = require 'wezterm'
local config = wezterm.config_builder()
local act = wezterm.action

config.keys = {
  {
    key = 'UpArrow',
    mods = 'CTRL|SHIFT',
    action = act.SendString('\x1b[<64;1;1M'),
  },
  {
    key = 'DownArrow',
    mods = 'CTRL|SHIFT',
    action = act.SendString('\x1b[<65;1;1M'),
  },
}

return config
```

おそらく、他の端末でもキーマップをある程度カスタマイズ性があるものであれば可能でしょう。
